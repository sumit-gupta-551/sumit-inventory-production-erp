name: sssj
description: ERP Inventory & Production Management
publish_to: 'none'

version: 1.0.01+18

environment:
  sdk: ">=3.2.6 <5.0.0"

dependencies:
  month_picker_dialog: ^2.0.2
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.6

  # DATE / FORMAT
  intl: ^0.18.1

  # LOCAL DATABASE
  sqflite: ^2.3.2
  sqflite_common_ffi: ^2.3.3
  sqlite3_flutter_libs: ^0.5.24
  path: ^1.8.3

  # 🔥 FIREBASE (CORRECT POSITION)
  firebase_core: ^3.12.1
  cloud_firestore:  ^5.6.7
  firebase_database: ^11.3.5
  firebase_auth: ^5.7.0
  # FILE / PDF / EXPORT
  pdf: ^3.10.8
  printing: ^5.12.0
  file_picker: ^8.0.0+1
  excel: ^4.0.6
  path_provider: ^2.1.2
  share_plus: ^7.2.2

  # FEATURES
  url_launcher: ^6.3.0
  telephony: ^0.2.0
  permission_handler: ^11.3.1
  speech_to_text: ^7.3.0
  flutter_foreground_task: ^9.2.2
  shared_preferences: ^2.2.3
  local_auth: ^2.2.0
  crypto: ^3.0.3
  open_filex: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.14.3

dependency_overrides:
  speech_to_text_windows:
    path: third_party/speech_to_text_windows

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/mslogo.png"
  min_sdk_android: 21
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/mslogo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/sssj.png
    - assets/mslogo.png
